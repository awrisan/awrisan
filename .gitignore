# Rust and Soroban build output
/target/

# Local Stellar identities and deployed runtime configuration
# .stellar: commit the on-chain evidence, never the keys
/.stellar/*
!/.stellar/rooms.json
!/.stellar/awrisan-runtime.json
/.stellar/identity/
/.stellar/github-deployment/

# Frontend dependencies and builds
/app/node_modules/
/app/dist/
/app/.npm-cache/
/app/android/.gradle/
/app/android/build/
/app/android/app/build/
/app/android/local.properties

# Local environment files
.env
.env.*
!.env.example

# Logs
*.log
